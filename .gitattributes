# 默认统一为 LF，与 Linux CI 保持一致
* text=auto eol=lf

# Windows 专属脚本保留 CRLF，避免 PowerShell 解析异常
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.exe binary
*.zip binary
